name: Hello World!

on:
    push:
        branches:
            - main

jobs:
    hello_world_job:
        runs-on: ubuntu-latest

        steps:
            - name: Say Hello
              run: |
                  echo "Hello, World!"

            - name: Now it is
              run: |
                  date

            - name: Directory content
              run: |
                  ls -l

            - name: Git Version
              run: |
                  git --version

            - name: Show My Name
              run: |
                  echo "My name is Ezeugo Felistus Obieze"
